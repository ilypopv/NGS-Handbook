[project]
name = "ngs-handbook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ilya Popov", email = "ilypopv@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ngs-handbook = "ngs_handbook:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.57",
]
